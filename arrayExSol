def sumDiagonal(a):
    # TODO
    n = len(a)
    sum = 0
    j = 0

    for i in range(n):
        sum += a[i][j]
        j += 1
        
    print(sum)
    return sum
        # for j in range(i+1,n):
        #     print(a[i],a[j])